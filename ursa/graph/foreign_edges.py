import ray
import numpy as np
from .utils import _filter_remote

MAX_SUBLIST_SIZE = 10

class ForeignEdges(object):


    def __init__(self, edges):
        #takes in dict

    def append(self, values):

    def filter(self, filterfn):


